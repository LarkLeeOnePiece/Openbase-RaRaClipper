# RaRaClipper - 3D 高斯点云的高级裁剪平面系统

[![License](https://img.shields.io/badge/License-非商业使用-blue.svg)](#许可证)
[![Python](https://img.shields.io/badge/Python-3.8+-green.svg)](#系统要求)
[![CUDA](https://img.shields.io/badge/CUDA-11.0+-orange.svg)](#系统要求)
[![PyTorch](https://img.shields.io/badge/PyTorch-2.0+-red.svg)](#安装)

**中文** | [English](README_EN.md)

## 📖 概述

RaRaClipper 是论文 **"RaRa Clipper: 基于光线追踪器和光栅化器的高斯点云裁剪器"** 的官方开源实现。本项目扩展了 [Splatviz](https://github.com/Florian-Barthel/splatviz)，为 3D 高斯点云提供了先进的多平面裁剪系统。

### 为什么选择 RaRaClipper?

传统的 3D 高斯点云裁剪方法容易产生视觉伪影和不一致性。RaRaClipper 引入了：

- **双策略裁剪** - 结合光线追踪和光栅化方法
- **1D 高斯表示** - 精确计算裁剪平面交集
- **交互式可视化** - 实时参数调整
- **修改的 CUDA 光栅化器** - 自定义裁剪逻辑

---

## 🔗 相关链接

- 📄 **论文：** [占位符：论文标题]
  - 会议：[占位符：会议名称] 20XX
  - DOI：[占位符：DOI]
  - PDF：[占位符：ArXiv/会议链接]

- 🏗️ **基于：**
  - [Splatviz](https://github.com/Florian-Barthel/splatviz) - 交互式查看器框架
  - [3D Gaussian Splatting](https://github.com/graphdeco-inria/gaussian-splatting) - 原始高斯点云实现
  - [占位符：多层高斯论文] - 多层数据格式

---

## ✨ 核心特性

### 裁剪策略

#### 1. **RR 策略（光线-光栅化）**
- 结合光线追踪和光栅化方法
- 使用 3 倍尺度阈值过滤高斯
- 在保持质量的同时优化性能

```python
# 在 GUI 中启用 RR 策略
rr_strategy = True
scale = 3.0  # 3 倍尺度阈值
```

#### 2. **1D GS 策略（一维高斯）**
- 将 3D 高斯投影到沿光线方向的 1D 表示
- 精确计算裁剪平面交集
- 减少裁剪边界的伪影

```python
# 在 GUI 中启用 1D GS 策略
oenD_gs_strategy = True
```

#### 3. **RR 裁剪（组合模式）**
- 自动启用两种策略
- 最佳质量，最少伪影
- 推荐用于大多数用例

```python
# 启用 RR 裁剪（自动启用两种策略）
rr_clipping = True
```

### 交互式裁剪平面小部件

- **多平面支持** - 同时定义多个裁剪平面
- **实时可视化** - 将裁剪平面显示为彩色多边形
- **交互式编辑** - 使用滑块调整平面法线和距离
- **盒子尺度控制** - 调整可视化边界框

### 三种操作模式

| 模式 | 描述 | 使用场景 |
|------|------|---------|
| **Default（默认）** | 标准 .ply 文件查看器 | 通用高斯点云场景 |
| **Decoder（解码器）** | 高斯 GAN 解码器支持 | 生成模型（.pkl 文件）|
| **Attach（附加）** | 远程渲染器附加 | 分布式渲染 |

---

## 🛠️ 安装

### 系统要求

- **操作系统：** Windows / Linux / macOS（需要 CUDA 支持）
- **GPU：** NVIDIA GPU，支持 CUDA（计算能力 ≥ 7.0）
  - 推荐：RTX 3000/4000 系列或更高
- **CUDA：** 11.0 或更高（推荐 11.8 或 12.1）
- **cuDNN：** 8.0 或更高（推荐）
- **Python：** 3.8、3.9 或 3.10
- **内存：** 最低 8GB，推荐 16GB
- **显存：** 最低 6GB，推荐 8GB+

### 验证 CUDA 安装

安装前，请验证您的 CUDA 设置：

```bash
# 检查 NVIDIA 驱动
nvidia-smi

# 检查 CUDA 版本
nvcc --version

# 应显示 CUDA 11.x 或 12.x
```

### 步骤 1：克隆仓库

```bash
git clone [占位符：仓库 URL]
cd RaRaClipper
```

### 步骤 2：安装 PyTorch 和依赖

**重要：** 在继续安装 RaRaClipper 特定组件之前，请确保 Splatviz 能正常工作。

```bash
# 对于 CUDA 11.8
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 对于 CUDA 12.1
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 安装其他依赖
pip install imgui-bundle click numpy imageio loguru Pillow open3d
```

### 步骤 3：使用 RaRaClipper 补丁构建 CUDA 扩展

这是关键步骤，RaRaClipper 的自定义裁剪逻辑将在此编译。

```bash
# 进入 diff-gaussian-rasterization 目录
cd gaussian-splatting/submodules/diff-gaussian-rasterization

# 构建并安装（这将编译包含裁剪逻辑的 forward.cu）
pip install -e .

# 进入 simple-knn 目录
cd ../simple-knn
pip install -e .

# 返回项目根目录
cd ../../..
```

**构建时间：** 根据系统配置，需要 5-10 分钟。

**正在编译的内容：**
- `cuda_rasterizer/forward.cu` - 修改后包含裁剪平面逻辑
- `cuda_rasterizer/backward.cu` - 梯度计算
- `rasterize_points.cu` - 主光栅化内核

### 步骤 4：验证安装

```bash
# 测试导入
python -c "import diff_gauss; print('✅ diff-gaussian-rasterization 安装成功！')"
python -c "import simple_knn; print('✅ simple-knn 安装成功！')"

# 测试完整应用程序
python run_main.py --help
```

### 完整安装脚本

为方便起见，这里提供完整的安装脚本：

```bash
#!/bin/bash
# install.sh

# 步骤 1：安装 PyTorch（CUDA 11.8）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 步骤 2：安装依赖
pip install imgui-bundle click numpy imageio loguru Pillow open3d

# 步骤 3：构建 diff-gaussian-rasterization
cd gaussian-splatting/submodules/diff-gaussian-rasterization
pip install -e .

# 步骤 4：构建 simple-knn
cd ../simple-knn
pip install -e .

# 步骤 5：返回根目录
cd ../../..

echo "✅ 安装完成！"
```

### 故障排除

**问题：CUDA 版本不匹配**
```bash
# 检查 PyTorch CUDA 版本
python -c "import torch; print(torch.version.cuda)"

# 应与系统 CUDA 版本匹配（±1 个版本通常可以）
```

**问题：构建失败，提示"CUDA not found"**
```bash
# 设置 CUDA_HOME 环境变量
export CUDA_HOME=/usr/local/cuda  # Linux/macOS
set CUDA_HOME=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v11.8  # Windows
```

**问题：编译时内存不足**
```bash
# 减少并行任务数
MAX_JOBS=4 pip install -e .
```

---

## 🚀 使用方法

### 基本用法

```bash
# 使用默认示例场景启动
python run_main.py

# 使用自定义 .ply 文件目录启动
python run_main.py --data_path=/path/to/your/ply/files

# 在解码器模式下启动（用于 .pkl 文件）
python run_main.py --data_path=/path/to/pkl/files --mode=decoder --ggd_path=/path/to/gaussian_decoder

# 在附加模式下启动（远程渲染器）
python run_main.py --mode=attach --host=192.168.1.100 --port=6009
```

### 命令行选项

| 选项 | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| `--data_path` | PATH | `./resources/sample_scenes` | 包含 .ply 或 .pkl 文件的目录 |
| `--mode` | STRING | `default` | 操作模式：`default`、`decoder`、`attach` |
| `--host` | STRING | `127.0.0.1` | 附加模式的主机地址 |
| `--port` | INT | `6009` | 附加模式的端口号 |
| `--ggd_path` | PATH | `` | 高斯 GAN 解码器项目路径（解码器模式）|

### 数据集格式

RaRaClipper 使用与 Splatviz 相同的格式：

```
data_path/
├── scene1/
│   └── point_cloud.ply          # 高斯点云 PLY 文件
├── scene2/
│   └── point_cloud.ply
└── scene3/
    └── compression_config.yml   # 压缩场景格式
```

**PLY 文件格式：** 标准 3D 高斯点云格式，包含以下属性：
- 位置：`x, y, z`
- 法线：`nx, ny, nz`
- 球谐函数：`f_dc_0, f_dc_1, f_dc_2, ...`
- 不透明度：`opacity`
- 尺度：`scale_0, scale_1, scale_2`
- 旋转：`rot_0, rot_1, rot_2, rot_3`（四元数）

---

## 🎮 GUI 控制

### 小部件概览

GUI 由多个可折叠的小部件组成：

| 小部件 | 描述 |
|--------|------|
| **Load（加载）** | 加载 .ply/.pkl 文件并管理场景 |
| **Camera（相机）** | 相机控制和视图管理 |
| **Performance（性能）** | FPS 计数器和内存使用情况 |
| **Video（视频）** | 录制旋转视频 |
| **Capture（捕获）** | 截图捕获 |
| **Render（渲染）** | 渲染选项（深度、透明度等）|
| **Edit（编辑）** | 用于场景编辑的 Python 代码执行 |
| **Eval（评估）** | 评估指标和质量评估 |
| **Clipping Plane（裁剪平面）** | 多平面裁剪控制（RaRaClipper 特性）|

### 裁剪平面小部件

<p align="center">
  <img src="resources/images/clipping_widget.png" width="400" alt="裁剪平面小部件"/>
  <br>
  <em>[占位符：添加裁剪平面小部件截图]</em>
</p>

#### 新平面配置

1. **设置平面法线** - 使用 `nx`、`ny`、`nz` 滑块（-1.0 到 1.0）
   - 归一化的法向量会自动显示
2. **设置平面距离** - 使用 `d` 滑块（-5.0 到 5.0）
   - 平面方程：`nx*x + ny*y + nz*z + d = 0`
3. **点击"Add Plane"** - 创建裁剪平面

#### 平面管理

- **编辑现有平面** - 使用滑块调整法线和距离
- **移除平面** - 点击每个平面旁边的"Remove"按钮
- **可视化平面** - 启用"vizPlane"复选框以查看裁剪平面多边形

#### RR 裁剪设置

- **RR Clipping** - 一键启用以获得最佳质量（自动启用 RR 策略 + 1D GS）
- **RR Strategy** - 启用光线-光栅化裁剪，使用 3 倍尺度阈值
- **1D GS Strategy** - 启用 1D 高斯表示以精确计算交集
- **Box Scale** - 调整可视化边界框大小（默认：1.0）

#### 策略比较

| 策略 | 质量 | 性能 | 伪影 | 使用场景 |
|------|------|------|------|---------|
| 无 | 基准 | 最快 | 很多 | 快速预览 |
| RR 策略 | 良好 | 快速 | 一些 | 通用 |
| 1D GS 策略 | 更好 | 中等 | 少量 | 注重质量 |
| RR 裁剪（两者） | 最佳 | 中等 | 最少 | 发表级质量 |

---

## 📸 示例

### 示例 1：单平面裁剪

```bash
python run_main.py --data_path=./resources/sample_scenes
```

1. 打开"Clipping Plane"小部件
2. 设置平面法线：`nx=1.0, ny=0.0, nz=0.0`
3. 设置距离：`d=0.0`
4. 启用"RR Clipping"
5. 启用"vizPlane"以查看裁剪平面
6. 调整 `d` 滑块以移动平面

<p align="center">
  <img src="resources/images/example_single_plane.png" width="800" alt="单平面裁剪"/>
  <br>
  <em>[占位符：添加单平面裁剪结果截图]</em>
</p>

### 示例 2：多平面裁剪

```bash
python run_main.py --data_path=/path/to/your/scene
```

1. 添加第一个平面：`normal=(1,0,0), d=0.5`
2. 点击"Add Plane"
3. 添加第二个平面：`normal=(0,1,0), d=-0.3`
4. 为两个平面启用"RR Clipping"
5. 调整平面以创建横截面视图

<p align="center">
  <img src="resources/images/example_multi_plane.png" width="800" alt="多平面裁剪"/>
  <br>
  <em>[占位符：添加多平面裁剪结果截图]</em>
</p>

### 示例 3：视频录制

1. 按需设置裁剪平面
2. 打开"Video"小部件
3. 配置相机路径（旋转/自定义）
4. 点击"Record Video"
5. 输出保存到 `./_videos/` 目录

<p align="center">
  <img src="resources/images/example_video.gif" width="800" alt="视频录制"/>
  <br>
  <em>[占位符：添加视频录制演示 GIF]</em>
</p>

---

## 🔧 技术细节

### 修改的 CUDA 光栅化器

RaRaClipper 修改了原始的 `diff-gaussian-rasterization`，添加了自定义裁剪逻辑：

**文件：`cuda_rasterizer/forward.cu`**

主要修改：
1. **`compute1DGS()` 函数** - 将 3D 高斯投影到 1D 表示
2. **`interval_probability_erf()` 函数** - 计算裁剪区间概率
3. **裁剪平面交集** - 测试每个高斯与平面的关系
4. **RR 策略过滤** - 应用 3 倍尺度阈值

**关键代码部分：**

```cuda
// 1D 高斯投影（第 59-96 行）
__device__ void compute1DGS(
    const float3& ray_origin,
    const float3& ray_dir,
    const float3& mu,
    const float3& scale,
    float mod,
    const float4& rot,
    float& mu_t,
    float& sigma_t)
{
    // 将 3D 高斯沿光线方向投影到 1D
    // 计算均值（mu_t）和方差（sigma_t）
}

// 裁剪概率计算（第 98+ 行）
__device__ float interval_probability_erf(
    float mean, float sigma, float a, float b)
{
    // 使用误差函数（erf）计算精确概率
}
```

### 性能考虑

| 场景大小 | 裁剪平面 | 策略 | FPS（RTX 3090） | 内存 |
|---------|---------|------|----------------|------|
| 小（<100K 高斯） | 1 | RR 裁剪 | ~60 FPS | ~2GB |
| 中（100K-500K） | 1 | RR 裁剪 | ~30 FPS | ~4GB |
| 大（>500K） | 1 | RR 裁剪 | ~15 FPS | ~8GB |
| 大（>500K） | 3 | RR 裁剪 | ~10 FPS | ~8GB |

**优化建议：**
- 使用更少的裁剪平面以获得更好的性能
- 不需要时禁用"vizPlane"
- 仅使用 RR 策略以获得更快的预览（禁用 1D GS）
- 关闭未使用的小部件以节省渲染开销

---

## 🧪 评估和指标

"Eval"小部件提供质量指标：

- **PSNR** - 峰值信噪比
- **SSIM** - 结构相似性指数
- **L1 Loss** - 平均绝对误差
- **Error Maps** - 可视化裁剪伪影

使用方法：
1. 加载真实场景
2. 应用裁剪平面
3. 打开"Eval"小部件
4. 查看指标和误差可视化

---

## 🐛 已知问题和限制

1. **内存使用** - 具有多个平面的大场景可能需要 >8GB 显存
2. **平面可视化** - 仅限于单个平面可视化（多平面开发中）
3. **平台兼容性** - 在 Windows/Linux 上测试最佳，macOS 支持为实验性
4. **CUDA 12.x** - 与最新 CUDA 版本存在一些兼容性问题

---

## 🗺️ 路线图

- [ ] 多平面同时可视化
- [ ] 裁剪平面预设库
- [ ] 批量处理用于评估
- [ ] 基于 Web 的查看器（无需 CUDA）
- [ ] Android/iOS 移动查看器

---

## 🤝 贡献

我们欢迎贡献！请：

1. Fork 仓库
2. 创建功能分支（`git checkout -b feature/amazing-feature`）
3. 提交更改（`git commit -m 'Add amazing feature'`）
4. 推送到分支（`git push origin feature/amazing-feature`）
5. 开启 Pull Request

---

## 📄 许可证

本项目基于 Gaussian Splatting，该项目根据 Inria 和 MPII 的**非商业研究许可证**授权。详见 [gaussian-splatting/LICENSE.md](gaussian-splatting/LICENSE.md)。

**主要限制：**
- ❌ 未经明确同意禁止商业使用
- ✅ 允许研究和评估使用
- ✅ 允许衍生作品（必须引用原作）

商业许可咨询，请联系：[占位符：联系邮箱]

---

## 📧 引用

如果您在研究中使用 RaRaClipper，请引用：

```bibtex
[占位符：添加完整 BibTeX]
@inproceedings{raraclipper2024,
  title={RaRa Clipper: 基于光线追踪器和光栅化器的高斯点云裁剪器},
  author={[占位符]},
  booktitle={[占位符：会议名称]},
  year={2024},
  organization={ACM}
}
```

同时引用基础工作：

```bibtex
# Splatviz
@software{splatviz2024,
  author = {Barthel, Florian},
  title = {Splatviz: Interactive 3D Gaussian Splatting Viewer},
  year = {2024},
  url = {https://github.com/Florian-Barthel/splatviz}
}

# 3D Gaussian Splatting
@article{kerbl20233d,
  title={3D Gaussian Splatting for Real-Time Radiance Field Rendering},
  author={Kerbl, Bernhard and Kopanas, Georgios and Leimk{\"u}hler, Thomas and Drettakis, George},
  journal={ACM Transactions on Graphics},
  volume={42},
  number={4},
  year={2023}
}
```

---

## 🙏 致谢

本项目的存在离不开：

- **Florian Barthel** - 优秀的 [Splatviz](https://github.com/Florian-Barthel/splatviz) 框架
- **INRIA GRAPHDECO** - [3D Gaussian Splatting](https://github.com/graphdeco-inria/gaussian-splatting)
- **[占位符：多层高斯作者]** - 多层数据格式
- **3D 高斯点云社区** - 持续的支持和反馈

---

## 📧 联系方式

- **GitHub Issues：** [占位符：Issues URL]
- **邮箱：** [占位符：联系邮箱]
- **Twitter：** [占位符：Twitter 账号]
- **项目主页：** [占位符：项目主页 URL]

---

<p align="center">
  用 ❤️ 为 3D 高斯点云社区制作
  <br>
  <br>
  ⭐ 如果您觉得有用，请在 GitHub 上给我们加星！
</p>
